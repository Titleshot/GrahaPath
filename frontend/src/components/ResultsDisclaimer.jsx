/**
 * Single calm framing line — keeps expectations aligned with structural analysis.
 */
export default function ResultsDisclaimer() {
  return (
    <p className="rounded-2xl border border-gold/10 bg-black/25 px-4 py-2.5 text-center text-[11px] leading-relaxed text-ivory/48">
      GrahaPath surfaces <span className="text-cream/65">patterns from calculated placements</span> — useful for
      reflection and timing context, not fixed fate or medical/legal advice.
    </p>
  );
}
