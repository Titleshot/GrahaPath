/**
 * Bridge path: implementation lives in `services/astroBrain/` (repo backend root).
 * Require from here if you prefer the `backend/services/astroBrain` layout.
 */
module.exports = require('../../../services/astroBrain/astroBrainService');
